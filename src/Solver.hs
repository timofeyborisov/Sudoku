module Solver
  ( solveStep
  , solveFull
  ) where

import Types

solveStep :: Board -> Maybe (SolverStrategy, Cell, Digit)
solveStep = undefined

solveFull :: Board -> Maybe Board
solveFull = undefined